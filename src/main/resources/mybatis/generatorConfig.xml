<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd" >
<generatorConfiguration>
    <properties resource="config/jdbc.properties"/>

    <classPathEntry location="${gen_driver}"/>

    <context id="DB2Tables" targetRuntime="MyBatis3" defaultModelType="flat">
        <!--<plugin type="com.shadow.mybtais.DeleteLogicByIdsPlugin"/>-->
        <!--<plugin type="org.mybatis.generator.plugins.RowBoundsPlugin"/>-->
        <!--<plugin type="com.shadow.mybtais.QuerySelectivePlugin"/>-->
        <plugin type="com.shadowyy.myabtis.CustomPlugin"/>

        <!-- 是否去除自动生成的注释 -->
        <commentGenerator>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <jdbcConnection driverClass="${driver}" connectionURL="${url}" userId="${username}" password="${password}"/>

        <javaTypeResolver>
            <!-- 默认false，把JDBC DECIMAL 和 NUMERIC 类型解析为 Integer。而设置为true则把JDBC DECIMAL 和 NUMERIC 类型解析为java.math.BigDecimal -->
            <property name="forceBigDecimals" value="false"/>
        </javaTypeResolver>

        <!-- 生成模型的包名和位置 -->
        <javaModelGenerator targetPackage="${package_domain}" targetProject="${gen_java}">
            <!-- 是否让schema作为包的后缀 -->
            <property name="enableSubPackages" value="true"/>
            <!--set的时候是否去除尾部空格 -->
            <property name="trimStrings" value="true"/>
        </javaModelGenerator>

        <!-- 生成映射文件的包名和位置 -->
        <sqlMapGenerator targetPackage="${package_mapper}" targetProject="${gen_resources}">
            <property name="enableSubPackages" value="true"/>
        </sqlMapGenerator>

        <!-- 生成DAO的包名和位置 -->
        <javaClientGenerator type="XMLMAPPER" targetPackage="${package_dao}" targetProject="${gen_java}">
            <property name="enableSubPackages" value="true"/>
        </javaClientGenerator>

        <!-- tables start-->
        <table tableName="${table_name}" domainObjectName="${domain_name}" enableCountByExample="false" enableUpdateByExample="false" enableDeleteByExample="false"
               enableSelectByExample="false" selectByExampleQueryId="false">
            <!-- 忽略该字段(可省略) -->
            <!-- <ignoreColumn column="name" /> -->
            <!--<generatedKey column="id" sqlStatement="JDBC" identity="true"/>-->
        </table>
        <!-- tables end-->
    </context>
</generatorConfiguration>
